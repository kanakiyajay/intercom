'use strict';

angular.module('tpApp')
  .factory('User', function ($resource) {
    return $resource('/api/employees/:id/:controller', {
      id: '@_id'
    },
    {
      changePassword: {
        method: 'PUT',
        params: {
          controller:'password'
        }
      },
      get: {
        method: 'GET',
        params: {
          id:'me'
        }
      }
	  });
  });
